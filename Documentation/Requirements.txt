Synthetic engineering requirements; not internal production calibration.
Revision 2.0; 2026-09-10.

NEP-11-001: inclusive raw-code bounds and nonnegative age <= timeout; invalid status rejects input.
NEP-11-002: reset clears qualification; two consecutive valid samples are required for recovery.
NEP-11-003: physical output uses explicit type/scale/offset and physical fallback.
NEP-11-004: domain components follow BusinessBehavior.md; independent properties and point cases are mandatory.
NEP-11-005: Capture input is reserved and has no effect; this module has no snapshot output.

NEP-11-SIG-001: decode uint16 raw code with physical=raw*0.1+0.0 V; reject stale/invalid communication; two valid samples restore qualified output.
NEP-11-SIG-002: decode uint16 raw code with physical=raw*0.1+0.0 V; reject stale/invalid communication; two valid samples restore qualified output.
NEP-11-SIG-003: decode uint32 raw code with physical=raw*1.0+0.0 ohm; reject stale/invalid communication; two valid samples restore qualified output.
NEP-11-SIG-004: decode boolean raw code with physical=raw*1.0+0.0 boolean; reject stale/invalid communication; two valid samples restore qualified output.
NEP-11-SIG-005: decode boolean raw code with physical=raw*1.0+0.0 boolean; reject stale/invalid communication; two valid samples restore qualified output.
NEP-11-SIG-006: decode boolean raw code with physical=raw*1.0+0.0 boolean; reject stale/invalid communication; two valid samples restore qualified output.
NEP-11-SIG-007: decode boolean raw code with physical=raw*1.0+0.0 boolean; reject stale/invalid communication; two valid samples restore qualified output.
NEP-11-SIG-008: decode boolean raw code with physical=raw*1.0+0.0 boolean; reject stale/invalid communication; two valid samples restore qualified output.
